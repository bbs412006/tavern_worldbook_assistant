node_modules

.DS_Store
Thumbs.db

*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Generated type declarations are tracked because vue-tsc needs them in clean CI clones.

# Webpack source maps are local debugging artifacts, not release inputs.
*.map

# Browser-test outputs are local verification artifacts.
test-results/
playwright-report/
